# Mem-eng Versions

- `mem-eng-linked-accounts-v1.0.1`: Fix Google merge callbacks opened inside LINE without overwriting the Google session.
- `mem-eng-linked-accounts-v1`: LINE login, safe Google account merge, and duplicate-word consolidation that preserves review progress.
- `mem-eng-line-sync-stable-v1` (`8080561`): Stable LINE chat, LIFF sign-in, secure deck sync, and private LINE words.
- `mem-eng-line-sprint-v1` (`5916e12`): First production-ready LINE bot and LIFF sprint.
- `mem-eng-full-v1` / `v2.0.0` (`6a5adad`): Full web app with the selected v0.2 improvements and LINE baseline.
- `v1.1.0-line-baseline` (`921b8ee`): LINE save-to-deck postback baseline.
- `v1.0.0` / `v0.1` (`a6c5494`): Original stable production app before the LINE expansion.
- `v0.2` (`2d92cd2`): Flashcard swipe ratings available at every reveal stage.
- `v0.21` (`edc036b`): Touch-device performance experiment.

Restore example: `Restore Mem-eng to tag mem-eng-line-sync-stable-v1 without changing the tag itself.`
